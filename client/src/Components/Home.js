import Feed from "./Feed";
import {Container, Box} from "@mui/material";
function Home({posts}) {
  return (
    // <main className="Home">
   <Box sx={{ display: "flex" }}>
      <h1> HOME sweet Home </h1>
    </Box>
  );
}

export default Home;
